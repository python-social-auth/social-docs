version: 2

sphinx:
  configuration: docs/conf.py
  fail_on_warning: true

python:
  install:
  - method: uv
    command: sync
    groups:
    - docs

build:
  os: ubuntu-24.04
  tools:
    python: '3.14'
